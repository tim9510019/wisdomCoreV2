# ARC-AGI-3 Agent Benchmark & Bug Fixes Report (FIX_260611)

本報告總結了在 `dir1_lora` 中所做的關鍵 Bug 修復，以及針對 `s5i5` 遊戲進行的 100 步完整測試結果與深度分析。

---

## 1. 關鍵 Bug 修復說明 (Bug Fixes Breakdown)

在之前的版本中，Agent 遇到了多個導致探索陷入死胡同、動作佇列被覆寫以及座標點擊錯誤的關鍵 Bug。我們針對這些問題進行了以下重構與修復：

### 1.1. Enum 單例模式引起的佇列覆寫問題 (Enum Singleton Queue Mutation Fix)
* **問題背景：** 原本的 `self.action_queue` 直接儲存 `GameAction` 物件。由於 Python 的 `Enum` 欄位是單例（Singleton），當我們在迴圈中對 `GameAction.ACTION6` 調用 `set_data({"x": cx, "y": cy})` 時，會修改所有在佇列中指向該單例的動作數據，導致整個佇列中的所有點擊動作最後都變成了最後一步的座標。
* **修復方案：** 重構佇列儲存方式。改為儲存純文字與字典組成的元組 `(action_name, action_data_dict, reasoning)`。僅在從佇列中取出執行（`popleft`）時，才動態生成 `GameAction` 並將 `action_data` 注入，徹底消除了單例變異帶來的覆寫問題。

### 1.2. 空間連通性 BFS 元件偵測重構 (Spatial Components BFS Refactoring)
* **問題背景：** 原版的元件偵測是基於「同色」進行 BFS 分組。這會導致複合型的控制介面（例如包含邊框色、背景色、分割線色等多種顏色的 Slider 或 Rotator）被拆散為多個零碎的獨立物件，導致 Agent 無法正確辨識出這是一個整體的控制條。
* **修復方案：** 重構 `perform_bfs_sequence_search`。將 BFS 的分組邏輯改為**僅依據空間八連通性（Spatial Connectivity）**，忽略顏色差異（只要不是背景色即可歸為同一物件）。這使得多色的複合型 Slider 與 Dial 能被完美辨識為單一的幾何控制元件。

### 1.3. 主導顏色過濾規則 (Dominant Color Filtering)
* **問題背景：** 滑軌的指針或選鈕的指針往往與主形狀（Movable Shape）或目標（Target Marker）擁有相同的顏色。若直接使用顏色辨識，會將這些可移動的幾何結構誤判為控制介面本體，造成 BFS 模擬器產生幻覺。
* **修復方案：** 引入主導顏色（Dominant Color）計算。一個空間連通物件的「主導顏色」為該物件中出現頻率最高的顏色。僅當物件的主導顏色既不是目標顏色（Target Color）也不是移動形狀顏色（Movable Color）時，才將該物件歸類為「控制介面（Control Component）」，大幅提升了模擬的準確度。

### 1.4. 自動候選點生成 (Auto Candidates Generation)
* **問題背景：** 大語言模型（LLM）有時無法精確輸出控制條的邊緣或對稱座標，導致 BFS 的候選點（Candidates）不夠優化。
* **修復方案：** 在代碼中加入了滑軌和旋鈕的自動候選點計算。針對水平滑軌，自動生成其中心點左右位移的點（`center_x ± 3`）；針對垂直滑軌，生成上下位移的點（`center_y ± 3`）；針對旋鈕，自動以中心點作為候選。這確保了即使 LLM 輸出的座標不精準，BFS 搜尋器也能在最優的控制點上進行探索。

---

## 2. 100 步測試執行結果 (100-Step Benchmark Results)

我們成功運行了 100 步的完整基準測試：
* **執行命令：** `.venv/bin/python3 scripts/run_benchmark.py --direction dir1_lora --games s5i5 --max-steps 100`
* **測試結果：**
  * **執行總步數：** 101 步
  * **執行狀態：** `GameState.GAME_OVER`（成功運行至上限步數，未發生 any Python Crash 或記憶體溢出）
  * **關卡通過數：** 0
  * **運行時間：** 650.6 秒 (約 10.8 分鐘)

---

## 3. 測試過程深度分析與關鍵發現 (Deep Dive & Key Findings)

雖然 Agent 能夠穩定且乾淨地跑完 101 步而沒有程式崩潰，但未能在 `s5i5` 中成功過關。透過對日誌進行深度分析，我們發現了一個關鍵的**「模擬器與攔截器死鎖循環」**問題：

### 3.1. 死鎖循環現象
以日誌中的 Step 98 - 100 為例：
1. **BFS 模擬器輸出決策：** `BFS simulation found optimal sequence of 2 steps: [(10, 34), (27, 10)]`。其中第一步是點擊 `(10, 34)`。
2. **啟發式攔截器阻截：** `Heuristic Intercept: Model choice 'ACTION6(x=10, y=34)' has already failed. Intercepting...`。
3. **執行隨機探索備選方案：** `Executing fallback exploration: 'ACTION6' (Reasoning: Click rarest color)`。這導致點擊了不相關的點（例如 `(10, 51)`），結果為 `No change.`。
4. **下一輪循環：** 由於上一步的隨機點擊沒有改變網格狀態，在下一步中，網格狀態依然與之前相同。BFS 模擬器再次執行，模擬結果依然顯示點擊 `(10, 34)` 是最優解，於是再次輸出 `(10, 34)`。
5. 攔截器再次阻截，再次進行隨機點擊，陷入無窮迴圈直到步數耗盡。

### 3.2. 根本原因分析 (Root Cause)
1. **滑軌機制的局限：** 在 `s5i5` 這類滑軌遊戲中，當滑軌指針已經處於正中心（例如 `x=10`）時，點擊正中心 `(10, 27)` 或 `(10, 34)` 不會產生任何變化（反應為 `No change.`），必須點擊滑軌的兩端（如 `x=9` 或 `x=11`）才能移動。
2. **模擬器未過濾已失效點：** BFS 模擬器的 `perform_bfs_sequence_search` 函數在生成 `all_candidates` 時，**並沒有與 `failed_actions` 進行對比過濾**。因此模擬器仍然會把已確定無效的點點放入模擬中。
3. **模擬器的啟發式偏差：** 模擬器在模擬點擊時，不管指針在哪裡，只要點擊控制條，就認為會朝某個方向移動（`shift_x = 3 if cx > obj_center_x else -3`）。這使得模擬器產生了「點擊中心點也能移動形狀」的樂觀假設，導致其不斷推薦已失效的點。

---

## 4. 下一步優化建議 (Recommendations for Future Improvement)

為了解決上述死鎖問題，建議在後續的 Agent 版本中加入以下優化：

1. **動作感知候選點過濾 (Action-Aware Candidate Filtering)：**
   在 `perform_bfs_sequence_search` 開始時，將已經記錄在 `failed_actions` 中的所有座標從 `all_candidates` 中剔除。這會迫使 BFS 尋找其他可行的控制路徑（例如點擊滑軌兩端 `(9, 27)` 或 `(11, 27)` 而非中心點）。
2. **動態更新模擬器行為：**
   當某個點擊被證實為 `No change.` 時，在後續的 BFS 模擬中，對該點的模擬點擊應返回原網格（不發生位移），使模擬器認識到該點在當前狀態下無效，從而修正優化路徑。

---
*報告完成日期：2026-06-11*
