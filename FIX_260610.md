# FIX_260610.md — direction1_lora 修復日誌

**日期：** 2026-06-10  
**目標遊戲：** s5i5（首要）  
**修復對象：** `direction1_lora/my_agent_lora.py`（同時套用至所有 5 個 agent）

---

## FIX-01：`grid_to_text_representation` 致命 Bug 修復

### 問題描述

**嚴重程度：CRITICAL** — 模型完全看不到任何遊戲物件座標。

```python
# ❌ 舊版（有 Bug）
if val != 0:  # 用 0 當「空」，但 s5i5 背景色是 5！
    non_zero.append(f"  - (row={y}, col={x}): Color {val}")
lines.extend(non_zero[:100])  # 前 100 格全是 Color 5（背景）

# 2D map 也被省略：
if H * W <= 1600:  # 64×64=4096 > 1600，所以完全不輸出 ASCII 地圖！
    ...
else:
    lines.append("(2D map omitted because grid is too large.)")
```

**後果：**
- s5i5 背景色為 Color 5（灰），佔 92.7%（3797/4096 格）
- 全部 4096 格皆為 non-zero → 前 100 個全是 `Color 5`（背景）
- 模型的文字輸入完全不含遊戲物件位置
- 2D ASCII map 因 64×64 > 1600 被完全省略
- 模型只能靠圖像猜測，但圖像解析受限，無法精確推斷座標

### 修復方案

```python
# ✅ 新版（已修復）
# 動態偵測背景色 = 最高頻色
unique_colors, counts = np.unique(grid, return_counts=True)
bg_color = int(unique_colors[np.argmax(counts)])

# 只列出「非背景」的格子（真正的遊戲物件）
if val != bg_color:
    important.append((y, x, val))

# 2D map：只印有物件的行（解決大網格省略問題）
interesting_rows = [y for y in range(H) if any(int(grid[y,x]) != bg_color ...)]
for y in interesting_rows:
    lines.append(f"  Row{y:02d}: " + "".join(...))
```

### 修復後效果（s5i5 Level 1 驗證通過）

```
Grid size: 64 rows x 64 columns
Background color (most frequent, ignore for interaction): Color 5
Non-background cell coordinates (299 total, these are the game objects):
  - (row=9, col=27): Color 3       ← 滑條邊框
  - (row=9, col=28): Color 14      ← 色彩選擇器
  ...
  - (row=34, col=10): Color 13     ← ★ 目標點(粉紅色)
  - (row=51, col=10): Color 13     ← ★ 目標標記(粉紅色)

2D map (· = background color 5, only rows with game objects shown):
  Row09: ···························3EEEEE···················D···········
  Row34: ·········BDB····················································
  Row51: ··········D·····················································
```

**驗證結果：** Color 13 ✅、Color 14 ✅、Color 2 ✅ — 模型現在能看到所有物件。

**套用範圍（全部通過語法驗證）：**
- `agent/my_agent.py` ✅
- `direction1_lora/my_agent_lora.py` ✅
- `direction2_hybrid/my_agent_hybrid.py` ✅
- `direction3_dna/my_agent_dna.py` ✅
- `direction4_dna_search/my_agent_dna_search.py` ✅

---

## FIX-02：System Prompt 去除初始偏見假設

### 問題描述

舊版 system prompt 包含硬編碼假設（如黃色方塊、特定座標），
導致模型跨遊戲產生錯誤先入為主的認知。

### 修復方案

將 system prompt 改為純方法論導向（META-EXPLORATION METHODS），
移除所有對特定顏色或座標的偏見描述。

現行 system prompt 四大探索方法：
1. **稀有度優先探索（PRIMARY）** — 優先點最稀有色的座標
2. **邊界與邊緣探索** — 目標邊框、轉換區
3. **對稱與結構探索** — 對稱軸、中心點
4. **離散動作系統測試** — 窮舉 ACTION1-7

---

## FIX-03：Heuristic Intercept 加入稀有度優先補完

### 問題描述

當模型輸出已失敗座標時，舊版直接隨機選動作（效率極低）。

### 修復方案

新增 `choose_rareness_exploration_action()`：
- 計算網格所有色彩的頻率，排除最高頻（背景）色
- 從最稀有色的座標依序嘗試（跳過已失敗的）
- 若 ACTION6 不可用，退回離散動作

```python
def choose_rareness_exploration_action(self, grid, failed_actions, available_actions):
    for color in sorted_colors:  # 從最稀有排起
        for x, y in coords_for_color:
            if f"ACTION6(x={x}, y={y})" not in failed_actions:
                return ACTION6(x, y)
```

**套用範圍：** 全部 5 個 agent 檔案 ✅

---

## s5i5 遊戲機制分析（破關必讀）

### 遊戲類型
**旋轉拼圖 + 尺寸調整配對遊戲**（非移動型、非射擊型）

### 勝利條件（原始碼 neurwiqfry 函式）
```python
# tag=0087vvmblxkzdi 的目標 sprite 需與 tag=0064ocqkuqacti 的標記點完全重疊
for 目標sprite in sprites_with_tag("0087vvmblxkzdi"):
    if not any(標記點.x == 目標.x and 標記點.y == 目標.y
               for 標記點 in sprites_with_tag("0064ocqkuqacti")):
        return False
return True  # 全部對齊 → 過關
```

### 關鍵 Sprite 角色對照表

| 代號 | 顏色 | Tag | 功能 | 互動方式 |
|------|------|-----|------|----------|
| `0086sibroshteg` | **Color 13（粉紅十字）** | `0087vvmblxkzdi` | 目標物件（需對齊標記點） | 無法直接點擊 |
| `0063ylopfyonpu` | **Color 13（粉紅點）** | `0064ocqkuqacti` | 目標標記點（固定） | 無法直接點擊 |
| `0088vigzkrxqiq` 等 | Color 9/14（小十字） | `0089rvqdprjwpz` | **色彩選擇器** → 點擊旋轉拼圖 | ✅ ACTION6 |
| `0065jkudrrwgvq` 等 | Color 2 外框 | `0066ghlkyvdbgg` | **長度滑條** → 左縮短右加長 | ✅ ACTION6 |
| `0000amgeiiukhc` 等 | 各種色 | `0001qwdmnlybkb` | 拼圖塊（被操作的對象） | 間接操作 |

### 正確操作流程

```
1. 看粉紅色標記點(Color 13, tag=0064)在哪
2. 看粉紅色目標(Color 13, tag=0087)在哪
3. 找對應顏色的「色彩選擇器」(tag=0089) → ACTION6 點擊 → 旋轉 90°
4. 找對應顏色的「長度滑條」(tag=0066) → ACTION6 點左半縮短/點右半加長
5. 重複直到目標 sprite 與標記點 XY 完全重疊
```

### 為何稀有度優先在 s5i5 效率不佳

- Color 13（最稀有色）是**靜態目標標記**（無法點擊）
- 真正需要點擊的「色彩選擇器」使用 Color 2（紅邊框）= **第 4 稀有色**
- 稀有度優先需遍歷至第 3-4 稀有色才能命中正確目標

---

## 待修復清單（TODO）

- [ ] **FIX-04：** 對 s5i5，BFS 候選座標應優先覆蓋 tag=0089rvqdprjwpz 和 tag=0066ghlkyvdbgg 的座標
- [ ] **FIX-05：** s5i5 需要序列操作（先點選擇器→再點滑條），單步 BFS 無法應對
- [ ] **FIX-06：** dc22 稀有度優先已確認失敗（最稀有色在非互動 sprite 中，不在 sys_click sprite）

---

*修復驗證日期：2026-06-10*  
*By: Antigravity Agent*
