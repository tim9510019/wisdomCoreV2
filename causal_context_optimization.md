# ARC-AGI-3: 因果上下文壓縮與二元分工架構 (Causal Context Compression & Agent-Backend Co-Design)

在 ARC-AGI-3 互動環境中，模型面臨最大的瓶頸是**上下文窗口中的雜訊過多**（例如：將每個像素的座標、大段的幾何物件描述塞入 Prompt，導致模型推理能力下降）。
為了解決這個難題，我們提出**二元分工（Agent-Backend Co-Design）**架構：由**後台（Python）負責幾何計算與模擬試探，將其提煉為極簡的「因果轉移表（Causal Transition Table）」，再提供給前台（Agent）進行宏觀決策**。

---

## 1. 二元分工架構 (Division of Labor)

### 後台 (Backend - Python 側)
* **職責：** 負責高頻、確定性的運算與空間感知。
  1. **物件分割 (Segmentation)**：以空間八連通性將網格分為獨立的物件。
  2. **類型分類 (Classification)**：以顏色頻率與歷史表現，將物件自動歸類為：`移動體`、`目標體`、`候選控制體`。
  3. **本地探測 (Local Simulation Probing)**：利用本地模擬器，對所有控制體上的候選座標進行模擬點擊。
  4. **因果歸納 (Causal Synthesis)**：將模擬結果中具有相同轉移效果的座標進行合併，生成極簡的因果表。

### 前台 (Agent - LLM 側)
* **職責：** 負責高階逻辑推理、長序列對齊規劃（PDCA）。
  1. **對齊差距分析 (Gap Analysis)**：觀察移動體與目標體的初始對齊差距（如：需要向左平移 6 格，順時針旋轉 90 度）。
  2. **動作選擇 (Macro-planning)**：從後台給出的極簡因果表中，挑選能填補差距的動作序列（如：執行動作 A 兩次，動作 C 一次）。
  3. **反饋校正 (PDCA Check)**：檢查真實環境回傳的狀態，若發生意外則修改意圖。

---

## 2. 上下文壓縮對比 (Context Compression Comparison)

### 原版 Prompt 資訊 (約 1500 - 2500 Tokens)
必須向 Agent 詳細描述：
* 網格中 20 多個物件的所有座標點。
* 滑軌的完整線段座標。
* 移動形狀的每一個像素。
* 複雜的 PDCA 指令。
*(缺點：模型容易在龐雜的座標數據中迷失，且無法精準計算點擊座標與幾何位移的數學關係)*

### 壓縮後的「因果對照表」Prompt (僅約 150 - 250 Tokens)
後台在本地模擬後，只給 Agent 輸出以下訊息：

```markdown
=== 幾何對齊Gap分析 ===
- 當前受控物體 (Movable Shape) 與目標 (Target) 之差距：
  * 平移 Gap: 需要水平向左移動 6 單位 (dx=-6)
  * 旋轉 Gap: 需要順時針旋轉 90 度 (rotation=+90°)

=== 可用控制動作因果表 (Causal Actions Table) ===
後台模擬已證實，點擊以下控制區域可對受控物體產生對應影響：
- [Action_A] 點擊水平滑軌左側 (推薦點: x=9, y=27)：
  * 效果: 受控物體水平向左平移 (dx=-3)
- [Action_B] 點擊水平滑軌右側 (推薦點: x=11, y=27)：
  * 效果: 受控物體水平向右平移 (dx=+3)
- [Action_C] 點擊旋鈕中心 (推薦點: x=52, y=10)：
  * 效果: 受控物體旋轉 (rotation=+90°)

=== 規劃要求 ===
請根據 Gap 分析，從 [Action_A, Action_B, Action_C] 中排出能填補 Gap 的動作佇列。
```

---

## 3. 是否讓 Agent 判斷工具列？ (Should Agent Identify Toolbars?)

**結論：不應該讓 Agent 在原生圖像/文字上直接做低階的「像素級座標判斷」，但應該讓 Agent 做高階的「語義確認」與「備選意圖指引」。**

我們設計的處理流程如下：

1. **後台自動探測與推薦（默認模式）：**
   * 後台以連通域演算法自動辨識出工具列（Actuators），並在背景完成模擬，生成上述的因果表。這是最節省 Token 且最高效的路徑。
   
2. **當自動偵測失敗或有歧義時，由 Agent 介入（備用模式）：**
   * 如果後台發現多個潛在的控制物件，但模擬器無法確定其因果關係，後台會給出精簡的物件清單：
     `"物件3: 位於 Rows 9-11, Cols 27, 顏色為 3。未探測出因果變化。"`
   * Agent 在決策時可以輸出指令：
     `"action": "PROBE", "target_object_id": 3`
   * 後台收到此指令後，會在真實環境（或引導模擬）中對該物件進行點擊探測，更新因果表，並在下一步呈現給 Agent。

這樣既保持了 Prompt 的極致簡潔，又保留了 Agent 作為 AGI 在遇到未知狀況時的主動探索能力。
